[package]
name = "testcrate"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
starlight = { path = "../starlight", features = ["debug", "u32_ptrs"] }

[dev-dependencies]
rand_xoshiro = { version = "0.8", default-features = false }
